{
  "name": "php-qr-generator",
  "version": "1.0.0",
  "description": "QR Code Generator PHP Application",
  "scripts": {
    "dev": "echo 'Use: docker-compose up -d && docker-compose exec web php -S localhost:8080 -t . server.php'",
    "start": "echo 'Use: docker-compose up -d && docker-compose exec web php -S localhost:8080 -t . server.php'",
    "setup": "echo 'Use: docker-compose exec web php setup-production.php'",
    "test": "echo 'Use: docker-compose exec web php simple-test.php'"
  },
  "dependencies": {},
  "devDependencies": {}
}